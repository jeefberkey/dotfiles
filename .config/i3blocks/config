# i3blocks config file
# vim: ft=dosini
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# Visit https://github.com/vivien/i3blocks-contrib for more!
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
#command=~/src/i3blocks-gaps/scripts/$BLOCK_NAME
#command=/usr/lib/i3blocks/$BLOCK_NAME
separator_block_width=8
separator=false
#border_top=0
#border_left=0
#border_right=0
#border_bottom=3
border=#6699cc

# only works on i3, and only kinda
#[focused_window]
#command=~/bin/ipc-window-title.rb
#interval=persist
#signal=12
##color=#A6D1FA
#format=json

[disk]
label= /
interval=30

[memory]
label=
interval=30

[load_average]
command=~/src/i3blocks-contrib/load_average/load_average
label=
interval=10

[temperature]
command=temperature --chip coretemp-isa-0000
label=
interval=10

[volume]
label=
instance=Master
#instance=PCM
interval=once
signal=10

[wireless]
label=
command=[[ $(iwgetid -r) == "" ]] && echo "Wifi Down" || iwgetid -r
color=#00FF00
interval=60
separator=false

[wifi]
instance=wlp58s0
interval=10

[brightness]
command=light
label=
interval=once
signal=11

[battery]
#command=batterybar
#markup=pango
interval=30

[time]
label=
command=date '+%A, %B %e %Y %l:%M %p'
color=#f9f8f5
interval=5

